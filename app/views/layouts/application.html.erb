<!DOCTYPE html>
<html>
  <head>
    <title>VovabularyApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
  
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2696508118331703"
     crossorigin="anonymous"></script>
  
     <%= csrf_meta_tags %>
     <%= csp_meta_tag %>

    <%= yield :head %>

    <%# --- デザインを復活させるための重要な行 --- %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%# --------------------------------------- %>

    <link rel="icon" href="/icon.png" type="image/png">
  </head>

  <body class="bg-gray-50 text-gray-900 font-sans">
    <%# フラッシュメッセージの表示エリア %>
    <div class="fixed top-5 left-0 right-0 z-50 flex flex-col items-center gap-2 pointer-events-none">
      <% flash.each do |type, message| %>
        <div class="<%= type == 'notice' ? 'bg-green-500' : 'bg-red-500' %> text-white px-6 py-3 rounded-2xl shadow-2xl font-bold pointer-events-auto">
          <%= message %>
        </div>
      <% end %>
    </div>

    <main class="container mx-auto mt-10 px-4">
      <%= yield %>
    </main>
  </body>
</html>
<footer class="mt-20 py-10 border-t border-slate-100 text-center text-xs text-slate-400">
  <div class="flex justify-center gap-4 mb-2">
    <%= link_to "利用規約", terms_path, class: "hover:underline" %>
    <%= link_to "プライバシーポリシー", privacy_path, class: "hover:underline" %>
  </div>
  <p>&copy; 2026 Vovabulary App</p>
</footer>