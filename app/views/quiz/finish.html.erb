<div class="max-w-2xl mx-auto px-4 py-12 text-center">
  <% if @all_mastered %>
    <%# --- 全単語マスター時の表示 (前回同様) --- %>
    <div class="bg-white rounded-3xl p-10 shadow-xl border border-slate-100 mb-8">
      <div class="text-6xl mb-6">🎉</div>
      <h2 class="text-3xl font-extrabold text-slate-800 mb-4">完全制覇！</h2>
      <p class="text-slate-600 mb-8">登録されている全ての単語をマスターしました！</p>
      <%= link_to "ホームに戻る", root_path, class: "inline-block bg-indigo-600 text-white px-8 py-4 rounded-2xl font-bold shadow-lg hover:bg-indigo-700 transition-all" %>
    </div>

  <% else %>
    <%# --- 通常のテスト完了画面 --- %>
    <div class="text-6xl mb-6">🏁</div>
    <h2 class="text-3xl font-extrabold text-slate-800 mb-8">Test Complete!</h2>

    <div class="grid grid-cols-2 gap-4 mb-10">
      <%# マスターした単語数カード %>
      <div class="bg-green-50 rounded-3xl p-6 border border-green-100 shadow-sm">
        <p class="text-xs font-bold text-green-600 uppercase tracking-widest mb-2">Mastered</p>
        <div class="text-4xl font-black text-green-700">
          <%= @mastered_count %><span class="text-lg ml-1">words</span>
        </div>
      </div>

      <%# 学習中の単語数カード %>
      <div class="bg-orange-50 rounded-3xl p-6 border border-orange-100 shadow-sm">
        <p class="text-xs font-bold text-orange-600 uppercase tracking-widest mb-2">Learning</p>
        <div class="text-4xl font-black text-orange-700">
          <%= @learning_count %><span class="text-lg ml-1">words</span>
        </div>
      </div>
    </div>

    <div class="flex flex-col gap-4">
      <%= link_to "もう一度テストする", quiz_path, class: "bg-indigo-600 text-white px-8 py-4 rounded-2xl font-bold shadow-lg hover:bg-indigo-700 transition-all" %>
      <%= link_to "ホームに戻る", root_path, class: "text-slate-400 hover:text-slate-600 font-semibold" %>
    </div>
  <% end %>
</div>